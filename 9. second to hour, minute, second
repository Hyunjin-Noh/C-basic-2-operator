#include <stdio.h>

int main()
{
    int total, hour, minute, second;
    scanf("%d", &total);
    hour = total / 3600;
    minute = (total % 3600) / 60; 
    second = (total % 3600) % 60;
    printf("%d시간 %d분 %d초\n", hour, minute, second);
    return 0;
}
